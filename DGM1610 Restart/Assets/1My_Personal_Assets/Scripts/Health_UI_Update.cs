using UnityEngine;
public class Health_UI_Update : MonoBehaviour
{
  /* public GUIStyle progress_empty;
   public GUIStyle progress_full;
   public float barDisplay;
   Vector2 pos = new Vector2(10,50);
   Vector2 size = new Vector2(250,250);
   public Texture2D emptyTex;
   public Texture2D fullTex;
   private void OnGUI()
   {
       GUI.BeginGroup(new Rect(pos.x, pos.y, size.x, size.y), emptyTex, progress_empty);
       GUI.Box(new Rect(pos.x, pos.y, size.x, size.y), emptyTex, progress_empty);
       //draw the filled-in part:
       GUI.BeginGroup(new Rect(0, 0, size.x * barDisplay, size.y));
       GUI.Box(new Rect(0, 0, size.x, size.y), fullTex, progress_full);
       GUI.EndGroup();
       GUI.EndGroup();
   }
   void Update()
   {
       //the player's health
       barDisplay = PlayerDisplay.playerHealth/PlayerDisplay.playerHealthMax;
   }   
   
   */
}